apiVersion: v1
kind: Service
metadata:
  name: {{ include "live-home-page.fullname" . }}
  labels:
    {{- include "live-home-page.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "live-home-page.selectorLabels" . | nindent 4 }}
