UniFi AI monitor deployed!

  Release : {{ .Release.Name }}
  Image   : {{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}

{{- if .Values.httpRoute.enabled }}
{{- range .Values.httpRoute.hostnames }}
  URL: https://{{ . }}
{{- end }}
{{- else if .Values.ingress.enabled }}
{{- range .Values.ingress.hosts }}
  URL: http{{ if $.Values.ingress.tls }}s{{ end }}://{{ .host }}
{{- end }}
{{- else if eq .Values.service.type "NodePort" }}
  Access via NodePort — get the node IP and port:
    kubectl get nodes -o wide
    kubectl get svc {{ include "unifi-ai.fullname" . }}
{{- else }}
  Port-forward to access locally:
    kubectl port-forward svc/{{ include "unifi-ai.fullname" . }} 4000:4000
  Then open http://localhost:4000
{{- end }}
